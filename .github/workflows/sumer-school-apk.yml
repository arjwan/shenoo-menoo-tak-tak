name: Sumer School APK
on:
  workflow_dispatch:
  push:
    paths:
      - 'android/sumer-school/**'
      - '.github/workflows/sumer-school-apk.yml'
jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build installable test APK
        run: gradle -p android/sumer-school assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: sumer-school-test-apk
          path: android/sumer-school/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
