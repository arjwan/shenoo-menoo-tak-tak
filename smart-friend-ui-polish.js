(function(){'use strict';
const API='https://shino-mino-tak-tak.duckdns.org';
const token=localStorage.getItem('token')||sessionStorage.getItem('token')||'';
function cleanReply(value){let text=String(value||'');for(const tag of ['think','analysis','reasoning']){const re=new RegExp('<'+tag+'\\b[^>]*>[\\s\\S]*?<\\/'+tag+'>','gi'),open=new RegExp('<'+tag+'\\b[^>]*>[\\s\\S]*$','gi');text=text.replace(re,'').replace(open,'');}text=text.replace(/(?:here(?:'s| is) a thinking process|thinking process|analysis|reasoning)\s*:?\s*[\s\S]*/gi,'').replace(/(?:أنا|اني)\s+(?:مجرد\s+)?(?:شخصية\s+)?(?:ذكاء\s+اصطناعي|نموذج\s+ذكاء\s+اصطناعي)[^.!؟\n]*(?:[.!؟]|$)/gi,' ').replace(/(?:جالس|موجود|عايش)\s+داخل\s+شنو\s+منو\s+تك\s+تك[^.!؟\n]*(?:[.!؟]|$)/gi,' ').replace(/(?:مو|مش|لست)\s+(?:إنسان(?:اً|ا)?|بشر(?:اً|ا)?|إنسان\s+حقيقي|انسان\s+حقيقي)[^.!؟\n]*(?:[.!؟]|$)/gi,' ').replace(/\s{2,}/g,' ').replace(/^\s*[،,.؛:!؟-]+\s*/,'').trim();return text;}
window.smartFriendCleanReply=cleanReply;
function cleanAll(){document.querySelectorAll('.sf-msg.ai > div,[data-sf-result]').forEach(el=>{const t=cleanReply(el.textContent);if(t!==el.textContent)el.textContent=t||'…';});}
function forceEnter(){document.querySelectorAll('[data-chat-form] textarea[name="text"],[data-sf-chat-form] textarea[name="text"]').forEach(input=>{if(input.dataset.sfEnterFixed==='1')return;input.dataset.sfEnterFixed='1';input.addEventListener('keydown',e=>{if(e.key!=='Enter'||e.shiftKey||e.isComposing)return;e.preventDefault();e.stopImmediatePropagation();const form=input.closest('form');if(form){if(typeof form.requestSubmit==='function')form.requestSubmit();else form.dispatchEvent(new Event('submit',{bubbles:true,cancelable:true}));}},true);});}
function imageData(file){return new Promise((resolve,reject)=>{if(!file||!file.type.startsWith('image/'))return reject(Error('اختار صورة أولاً'));const reader=new FileReader();reader.onerror=()=>reject(Error('تعذر قراءة الصورة'));reader.onload=()=>{const img=new Image();img.onerror=()=>reject(Error('تعذر فتح الصورة'));img.onload=()=>{const max=1200,scale=Math.min(1,max/Math.max(img.width,img.height)),c=document.createElement('canvas');c.width=Math.round(img.width*scale);c.height=Math.round(img.height*scale);c.getContext('2d').drawImage(img,0,0,c.width,c.height);resolve(c.toDataURL('image/jpeg',.8))};img.src=reader.result};reader.readAsDataURL(file)})}
async function req(path,opt={}){opt.headers=Object.assign({Authorization:'Bearer '+token,'Content-Type':'application/json'},opt.headers||{});const r=await fetch(API+path,opt),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||'تعذر تنفيذ الطلب');return d;}
function renderImageResults(holder,images){holder.innerHTML='';if(!images?.length){holder.textContent='ما لقيت صور مناسبة بهالبحث، جرّب صياغة ثانية.';return;}const grid=document.createElement('div');grid.style.cssText='display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:8px';images.forEach(item=>{const card=document.createElement('a');card.href=item.pageUrl||item.imageUrl||'#';card.target='_blank';card.rel='noopener';card.style.cssText='display:block;text-decoration:none;color:inherit;border:1px solid rgba(255,255,255,.12);border-radius:10px;overflow:hidden;background:rgba(255,255,255,.03)';const img=document.createElement('img');img.src=item.thumbUrl;img.alt=item.title||'صورة مقترحة';img.loading='lazy';img.style.cssText='width:100%;height:110px;object-fit:cover;display:block';const cap=document.createElement('div');cap.textContent=item.title||'صورة مناسبة';cap.style.cssText='font-size:11px;padding:6px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis';card.append(img,cap);grid.appendChild(card);});holder.appendChild(grid);}
function enhanceReview(){const panel=document.querySelector('[data-sf-panel="review"]'),result=document.querySelector('[data-sf-result]');if(!panel||panel.dataset.reviewEnhanced==='1')return;panel.dataset.reviewEnhanced='1';const textarea=document.createElement('textarea');textarea.setAttribute('data-sf-review-text','1');textarea.placeholder='الصق نص المنشور هنا، أو اتركه فارغاً لاستخدام منشور الصفحة الحالية';textarea.style.cssText='width:100%;min-height:120px;margin:8px 0;box-sizing:border-box';const file=document.createElement('input');file.type='file';file.accept='image/*';file.hidden=true;const pick=document.createElement('button');pick.type='button';pick.textContent='🖼 اختيار صورة من الجهاز';const chosen=document.createElement('div');chosen.style.cssText='font-size:12px;opacity:.8;margin:6px 0';const preview=document.createElement('img');preview.hidden=true;preview.style.cssText='width:100%;max-height:220px;object-fit:contain;border-radius:10px;margin:6px 0';pick.onclick=()=>file.click();file.onchange=()=>{const f=file.files?.[0];chosen.textContent=f?'تم اختيار: '+f.name:'';if(f){preview.src=URL.createObjectURL(f);preview.hidden=false;}else preview.hidden=true;};const reviewBtn=panel.querySelector('[data-sf-review]'),anchor=reviewBtn?.parentNode||panel.firstChild;panel.insertBefore(textarea,anchor);panel.insertBefore(file,anchor);panel.insertBefore(pick,anchor);panel.insertBefore(chosen,anchor);panel.insertBefore(preview,anchor);
const searchBtn=document.createElement('button');searchBtn.type='button';searchBtn.textContent='🔎 جيبلي صور مناسبة للمنشور';searchBtn.style.marginInlineStart='6px';const imageResults=document.createElement('div');imageResults.setAttribute('data-sf-image-results','1');imageResults.style.cssText='margin-top:8px';anchor.appendChild(searchBtn);panel.insertBefore(imageResults,anchor.nextSibling);
searchBtn.onclick=async()=>{const pasted=textarea.value.trim(),pageText=document.querySelector('[data-post-text]')?.value.trim()||'',text=pasted||pageText;if(!text){result.textContent='الصق نص المنشور أولاً حتى أبحث لك عن صور مناسبة';return;}result.textContent='دا أبحث لك عن صور مناسبة…';try{const d=await req('/api/smart-friend/tools/image-search',{method:'POST',body:JSON.stringify({text})});renderImageResults(imageResults,d.images||[]);result.textContent=d.images?.length?'لقيت لك صور مناسبة، اختار اللي يعجبك.':'ما لقيت صور مناسبة بهالمرة.';}catch(err){result.textContent=err.message}};
if(reviewBtn){reviewBtn.textContent='راجع النص أو الصورة';reviewBtn.addEventListener('click',async e=>{e.preventDefault();e.stopImmediatePropagation();const pasted=textarea.value.trim(),pageText=document.querySelector('[data-post-text]')?.value.trim()||'',text=pasted||pageText,image=file.files?.[0]||document.querySelector('[data-media]')?.files?.[0]||null;if(!text&&!image){result.textContent='الصق نص المنشور أو اختر صورة أولاً';return;}result.textContent=image?'دا أشوف الصورة وأقراها…':'دا أراجع النص…';try{let body;if(image&&image.type.startsWith('image/'))body={mode:'image',text,imageData:await imageData(image)};else body={mode:document.querySelector('[data-sf-review-mode]')?.value||'opinion',text};const d=await req('/api/smart-friend/tools/review',{method:'POST',body:JSON.stringify(body)});result.textContent=cleanReply(d.reply||d.message||'تمت المراجعة');}catch(err){result.textContent=err.message}},true);}}
function polishResult(){const result=document.querySelector('[data-sf-result]');if(!result)return;result.style.whiteSpace='pre-wrap';result.style.maxHeight='45vh';result.style.overflowY='auto';result.style.wordBreak='break-word';}
function init(){forceEnter();enhanceReview();polishResult();cleanAll();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();new MutationObserver(init).observe(document.documentElement,{subtree:true,childList:true,characterData:true});
})();